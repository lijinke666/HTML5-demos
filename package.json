{
  "scripts": {
    "start": "browser-sync start --server --files '**/*.html, **/*.css' ",
    "pwa": "cd PWA && http-server -c-1",
    "bookmark": "bookmark -p /HTML5-demos/"
  },
  "dependencies": {
    "browser-sync": "^2.18.12",
    "j-bookmark": "^0.2.1"
  },
  "devDependencies": {
    "pre-commit": "^1.2.2"
  }
}
